{
  "name": "clean-bookmarks",
  "type": "module",
  "private": true,
  "scripts": {
    "sync-models": "node scripts/sync-models.cjs",
    "build-manifest": "node scripts/build-manifest.cjs",
    "prebuild": "npm run sync-models && npm run build-manifest",
    "build": "node build.cjs",
    "typecheck": "npx tsc --noEmit",
    "test": "npx jest"
  },
  "devDependencies": {
    "@types/chrome": "latest",
    "@types/node": "^20",
    "esbuild": "^0.24",
    "typescript": "^5"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.78.0",
    "zod": "^4.4.3"
  }
}
