{
  "name": "clean-bookmarks",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "sync-models": "bun run scripts/sync-models.ts",
    "build-manifest": "bun run scripts/build-manifest.ts",
    "prebuild": "bun run sync-models && bun run build-manifest",
    "build": "bun build.ts",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.78.0",
    "zod": "^4.4.3"
  }
}
